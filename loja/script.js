function buyPackage(quantity, price) {
    // Aqui você pode adicionar a lógica para processar a compra, por exemplo, redirecionar para um gateway de pagamento.
    alert(`Você comprou ${quantity} gemas por R$ ${price.toFixed(2)}`);
}
